<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:layout_marginTop="16dp"
    android:background="@color/colorPrimary"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tv_name"
        android:layout_centerVertical="true"
        android:layout_margin="16dp"
        android:textColor="@color/whiteText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:width="200dp" />

    <ImageView
        android:id="@+id/iv_delete"
        android:src="@android:drawable/ic_menu_delete"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/delete_icon" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/podCountLabel"
        android:width="80dp"
        android:textColor="@color/whiteText"
        android:text="@string/pod_count"
        android:layout_alignTop="@+id/tv_name"
        android:layout_toRightOf="@+id/tv_name"
        android:layout_toEndOf="@+id/tv_name" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tvPodCount"
        android:width="10dp"
        android:hint="@string/pod_count_hint"
        android:textColorHint="@color/whiteText"
        android:textColor="@color/whiteText"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/podCountLabel"
        android:layout_toEndOf="@+id/podCountLabel"
        android:textIsSelectable="false" />

</RelativeLayout>